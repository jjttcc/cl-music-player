#!/bin/env ruby

require './configuration'
require './player'

configuration = Configuration.new
dbfile = configuration.dbfile
player = Player.new
ARGV.each do |arg|
  list = dbfile.matchesfor(arg)
  if list.length > 0
    player.playfiles(list)
  else
    $stderr.puts "No matches found for #{arg}"
  end
end
